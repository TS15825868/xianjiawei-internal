const state={me:null,items:[],filter:'',status:'all',busy:false,platforms:null};
const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=(v='')=>String(v).toLowerCase().replace(/\s+/g,'');
const fmt=v=>v?new Date(v).toLocaleString('zh-TW',{hour12:false,timeZone:'Asia/Taipei'}):'—';
function localInput(value){if(!value)return'';const d=new Date(value),p=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Taipei',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(d),m=Object.fromEntries(p.filter(x=>x.type!=='literal').map(x=>[x.type,x.value]));return`${m.year}-${m.month}-${m.day}T${m.hour}:${m.minute}`}
function taipeiLocalToIso(v){return v?new Date(`${v}:00+08:00`).toISOString():''}
function nextSlot(which='next'){const now=new Date(),out=[];for(let add=0;add<15;add++){const local=new Date(now.toLocaleString('en-US',{timeZone:'Asia/Taipei'}));local.setDate(local.getDate()+add);if(![2,6].includes(local.getDay()))continue;local.setHours(local.getDay()===2?19:9,30,0,0);const iso=taipeiLocalToIso(`${local.getFullYear()}-${String(local.getMonth()+1).padStart(2,'0')}-${String(local.getDate()).padStart(2,'0')}T${String(local.getHours()).padStart(2,'0')}:${String(local.getMinutes()).padStart(2,'0')}`);if(new Date(iso)>now)out.push(iso)}return out[which==='following'?1:0]||new Date(Date.now()+86400000).toISOString()}
async function api(path,options={}){const ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),Number(options.timeout||20000));try{const res=await fetch(`/api${path}`,{credentials:'same-origin',cache:'no-store',...options,signal:options.signal||ctl.signal,headers:{...(options.body instanceof FormData?{}:{'content-type':'application/json'}),...(options.headers||{})}});const text=await res.text();if((res.headers.get('content-type')||'').includes('text/html')||/^\s*<!doctype html/i.test(text))throw new Error('登入狀態已失效，請重新整理並完成 Cloudflare Access 登入。');let data=text;try{data=text?JSON.parse(text):null}catch{}if(!res.ok)throw new Error(data?.error||data?.detail||`系統連線失敗（HTTP ${res.status}）`);return data}catch(error){if(error?.name==='AbortError')throw new Error('系統回應逾時，請重新整理再試一次。');throw error}finally{clearTimeout(timer)}}
function toast(message,error=false){const root=$('#toastRoot');if(!root)return;const n=document.createElement('div');n.className=`toast ${error?'error':''}`;n.textContent=message;root.appendChild(n);setTimeout(()=>n.remove(),4200)}
function connection(text,error=false){const n=$('#connectionState');if(n){n.textContent=text;n.classList.toggle('error',error)}}
function statusLabel(v=''){return({draft:'草稿',pending_review:'待審核',approved:'已核准',scheduled:'已排程',published:'已發布',manual_required:'需人工發布',failed:'失敗',archived:'已封存'})[v]||v||'—'}
function audit(post){const copy=norm([post.title,post.headline,post.copy,post.category].join(' ')),image=norm([post.image_url,post.image_alt,post.image_source].join(' '));const rules=[['龜鹿膏',['龜鹿膏','gao']],['龜鹿飲30',['30cc','龜鹿飲30','drink-30']],['龜鹿飲180',['180cc','龜鹿飲180','drink-180']],['龜鹿湯塊',['湯塊','soup']],['鹿茸粉',['鹿茸粉','antler']],['龜鹿膠',['龜鹿膠','jiao']]],mentioned=rules.filter(([,keys])=>keys.some(k=>copy.includes(norm(k))));if(!post.image_url)return{level:'danger',text:'缺少圖片，不能通過審核。'};if(mentioned.length&&!mentioned.some(([,keys])=>keys.some(k=>image.includes(norm(k)))))return{level:'danger',text:`文案提到「${mentioned.map(x=>x[0]).join('、')}」，圖片資訊無法確認對應產品，請人工檢查。`};if(!post.image_alt)return{level:'warning',text:'尚未填寫圖片說明，請補齊後再完成審核。'};if(post.image_quality_status==='low')return{level:'danger',text:'圖片解析度不足，請更換清晰圖片。'};return{level:'ok',text:'基本圖文檢查通過；發布前仍需人工確認16項。'}}
function selectedItems(){const q=norm(state.filter);return state.items.filter(p=>(!q||norm(JSON.stringify(p)).includes(q))&&(state.status==='all'||p.status===state.status))}
function metrics(){const c={review:0,approved:0,scheduled:0,published:0,manual:0};for(const p of state.items){if(['draft','pending_review'].includes(p.status))c.review++;if(p.status==='approved')c.approved++;if(p.status==='scheduled')c.scheduled++;if(p.status==='published')c.published++;if(p.status==='manual_required')c.manual++}return c}
function renderMetrics(){const c=metrics();$('#metrics').innerHTML=[['待審核',c.review,'pending_review'],['已核准',c.approved,'approved'],['已排程',c.scheduled,'scheduled'],['已發布',c.published,'published'],['需人工發布',c.manual,'manual_required']].map(([label,value,status])=>`<button class="publish-metric ${state.status===status?'active':''}" data-filter-status="${status}"><small>${label}</small><strong>${value}</strong></button>`).join('')}
function renderPlatforms(){const root=$('#platformSummary');if(!root)return;const data=state.platforms?.platforms||{};const entries=Object.entries(data);root.innerHTML=entries.length?entries.map(([name,p])=>`<span class="platform-chip ${p.ready?'ready':'manual'}">${esc(name)}：${p.ready?'可自動發布':'人工／未授權'}</span>`).join(''):'<span class="platform-chip">平台狀態載入中</span>'}
function card(post){const a=audit(post),locked=post.status==='published';return`<article class="card xjw-row publish-card" data-status="${esc(post.status)}" data-locked="${locked?'true':'false'}">
<div class="xjw-row-head"><div><p class="eyebrow">${esc(post.category||'貼文')}</p><h3>${esc(post.title||'未命名貼文')}</h3></div><span class="status-pill ${esc(post.status)}">${esc(statusLabel(post.status))}</span></div>
<div class="xjw-meta"><span>${esc((post.platforms||[]).join('／')||'未指定平台')}</span>${post.scheduled_at?`<span>排程：${esc(fmt(post.scheduled_at))}</span>`:''}</div>
${post.headline?`<h4>${esc(post.headline)}</h4>`:''}<div class="xjw-copy">${esc(post.copy||'尚無文案')}</div>${post.image_url?`<img class="xjw-image-preview" src="${esc(post.image_url)}" alt="${esc(post.image_alt||post.title||'貼文圖片')}" loading="lazy">`:''}
<div class="xjw-${a.level}">${esc(a.text)}</div><div class="xjw-actions"><button class="btn small" data-post-view="${esc(post.id)}">查看／發布結果</button>${!locked?`<button class="btn small orange" data-post-edit="${esc(post.id)}">重新編輯</button>`:''}${['draft','pending_review'].includes(post.status)?`<button class="btn small green" data-post-status="approved" data-id="${esc(post.id)}" ${a.level==='danger'?'disabled':''}>審核通過</button>`:''}${post.status==='approved'?`<button class="btn small orange" data-post-schedule="${esc(post.id)}">安排時間</button><button class="btn small green" data-post-publish-now="${esc(post.id)}">立即發布</button>`:''}${post.status==='scheduled'?`<button class="btn small orange" data-post-schedule="${esc(post.id)}">修改時間</button><button class="btn small" data-post-status="draft" data-id="${esc(post.id)}">取消排程</button><button class="btn small green" data-post-publish-now="${esc(post.id)}">立即發布</button>`:''}${post.status==='manual_required'?`<button class="btn small" data-post-deliveries="${esc(post.id)}">查看各平台狀態</button>`:''}</div></article>`}
function renderList(){const list=selectedItems();$('#listRoot').innerHTML=list.length?`<div class="xjw-list">${list.map(card).join('')}</div>`:'<section class="card empty-state"><h3>沒有符合條件的貼文</h3><p>可調整篩選條件或新增貼文草稿。</p></section>';renderMetrics()}
async function load(){connection('連線中…');try{const [postsResult,platformResult]=await Promise.allSettled([api('/posts'),api('/platform-authorization')]);if(postsResult.status==='rejected')throw postsResult.reason;const data=postsResult.value;state.items=Array.isArray(data)?data:(data?.items||data?.results||data?.records||[]);state.platforms=platformResult.status==='fulfilled'?platformResult.value:null;renderList();renderPlatforms();connection(`已連線｜${state.items.length} 篇貼文`)}catch(error){connection('連線失敗',true);$('#listRoot').innerHTML=`<section class="card empty-state"><h3>貼文系統暫時無法載入</h3><p>${esc(error.message||error)}</p><button class="btn primary" data-refresh>重新連線</button></section>`}}
function closeModal(){$('#modalRoot').innerHTML=''}
function selectedPlatforms(form){return $$('[name="platforms"]:checked',form).map(i=>i.value)}
function openPostForm(post=null){const edit=!!post;$('#modalRoot').innerHTML=`<div class="xjw-modal"><div class="xjw-modal-bg" data-close-modal></div><form class="xjw-modal-card" id="postForm"><h2>${edit?'重新編輯貼文':'新增貼文草稿'}</h2>${edit&&post.status!=='draft'?'<div class="xjw-warning">儲存修改後會退回草稿，必須重新審核。</div>':''}<div class="form-grid"><label class="field full"><span>標題</span><input name="title" required value="${esc(post?.title||'')}"></label><label class="field full"><span>主標</span><input name="headline" value="${esc(post?.headline||'')}"></label><label class="field full"><span>文案</span><textarea name="copy" required>${esc(post?.copy||'')}</textarea></label><label class="field"><span>分類</span><input name="category" value="${esc(post?.category||'日常節奏')}"></label><label class="field"><span>圖片網址</span><input name="image_url" type="url" value="${esc(post?.image_url||'')}"></label><label class="field full"><span>圖片說明</span><input name="image_alt" value="${esc(post?.image_alt||'')}"></label><fieldset class="field full"><legend>發布平台</legend>${['Facebook','Instagram','LINE OA','LINE VOOM','Google 商家'].map(name=>`<label class="check-label"><input type="checkbox" name="platforms" value="${name}" ${(post?.platforms||['Facebook','Instagram']).includes(name)?'checked':''}> ${name}</label>`).join('')}</fieldset></div><div class="xjw-modal-footer"><button type="button" class="btn" data-close-modal>取消</button><button class="btn primary">${edit?'儲存並退回草稿':'儲存草稿'}</button></div></form></div>`;$('#postForm').addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,body=Object.fromEntries(new FormData(form).entries());body.platforms=selectedPlatforms(form);try{await api(edit?`/posts/${encodeURIComponent(post.id)}`:'/posts',{method:edit?'PUT':'POST',body:JSON.stringify(body)});closeModal();toast(edit?'貼文已更新並退回草稿':'貼文草稿已新增');await load()}catch(error){toast(error.message,true)}})}
async function deliveries(id){try{return await api(`/posts/${encodeURIComponent(id)}/deliveries`)}catch{return null}}
async function openPostView(post){const d=await deliveries(post.id),rows=d?.platforms||[],$modal=$('#modalRoot');$modal.innerHTML=`<div class="xjw-modal"><div class="xjw-modal-bg" data-close-modal></div><div class="xjw-modal-card"><h2>${esc(post.title||'貼文')}</h2><div class="xjw-meta"><span class="status-pill ${esc(post.status)}">${esc(statusLabel(post.status))}</span><span>${esc((post.platforms||[]).join('／'))}</span></div>${post.headline?`<h3>${esc(post.headline)}</h3>`:''}<div class="xjw-copy">${esc(post.copy||'尚無文案')}</div>${post.image_url?`<p><img class="xjw-image-preview" src="${esc(post.image_url)}" alt="${esc(post.image_alt||post.title)}"></p>`:''}<h3>平台發布結果</h3><div class="delivery-grid">${rows.length?rows.map(r=>`<div class="delivery-row"><strong>${esc(r.platform)}</strong><span>${esc(statusLabel(r.status))}</span>${r.error_text?`<small>${esc(r.error_text)}</small>`:''}</div>`).join(''):'<p class="muted">目前尚無平台發布紀錄。</p>'}</div><div class="xjw-modal-footer">${post.status!=='published'?`<button class="btn orange" data-post-edit="${esc(post.id)}">重新編輯</button>`:''}<button class="btn" data-close-modal>關閉</button></div></div></div>`}
function openSchedule(post){const suggested=post.scheduled_at||post.proposed_scheduled_at||nextSlot();$('#modalRoot').innerHTML=`<div class="xjw-modal"><div class="xjw-modal-bg" data-close-modal></div><form class="xjw-modal-card" id="scheduleForm"><h2>${post.status==='scheduled'?'修改排程時間':'安排貼文時間'}</h2><p>${esc(post.title)}</p><div class="xjw-schedule-presets"><button type="button" class="btn" data-schedule-preset="next">下一個固定時段</button><button type="button" class="btn" data-schedule-preset="following">再下一個固定時段</button><button type="button" class="btn green" data-publish-now-from-modal>立即發布</button></div><label class="field full"><span>日期與時間（台灣時間）</span><input id="scheduleAt" type="datetime-local" required value="${esc(localInput(suggested))}"></label><div class="xjw-warning">固定時段為週二19:30、週六09:30；立即發布不受固定時段限制。</div><div class="xjw-modal-footer"><button type="button" class="btn" data-close-modal>取消</button><button class="btn primary">儲存排程</button></div></form></div>`;const input=$('#scheduleAt');$$('[data-schedule-preset]').forEach(b=>b.addEventListener('click',()=>input.value=localInput(nextSlot(b.dataset.schedulePreset))));$('[data-publish-now-from-modal]')?.addEventListener('click',async()=>{closeModal();await publishNow(post.id)});$('#scheduleForm').addEventListener('submit',async e=>{e.preventDefault();await changeStatus(post.id,'scheduled',taipeiLocalToIso(input.value));closeModal()})}
async function changeStatus(id,status,scheduled_at=''){try{await api(`/posts/${encodeURIComponent(id)}/status`,{method:'POST',body:JSON.stringify({status,scheduled_at})});toast('貼文狀態已更新');await load()}catch(error){toast(error.message,true)}}
async function publishNow(id){if(state.busy)return;state.busy=true;toast('正在送出立即發布，請稍候…');try{const result=await api(`/posts/${encodeURIComponent(id)}/publish-now`,{method:'POST',body:'{}',timeout:45000});toast(result?.message||'立即發布完成');await load()}catch(error){toast(error.message,true)}finally{state.busy=false}}
async function loadMe(){try{state.me=await api('/me');$('#userState').textContent=state.me?.display_name||state.me?.email||'已登入'}catch(error){$('#userState').textContent='登入驗證失敗';toast(error.message,true)}}
function bind(){document.addEventListener('click',e=>{const t=e.target.closest('[data-close-modal]');if(t){closeModal();return}const refresh=e.target.closest('[data-refresh]');if(refresh){load();return}const add=e.target.closest('[data-add-post]');if(add){openPostForm();return}const f=e.target.closest('[data-filter-status]');if(f){state.status=state.status===f.dataset.filterStatus?'all':f.dataset.filterStatus;$('#statusFilter').value=state.status;renderList();return}const view=e.target.closest('[data-post-view],[data-post-deliveries]');if(view){const id=view.dataset.postView||view.dataset.postDeliveries,post=state.items.find(p=>p.id===id);if(post)openPostView(post);return}const edit=e.target.closest('[data-post-edit]');if(edit){const post=state.items.find(p=>p.id===edit.dataset.postEdit);if(post)openPostForm(post);return}const status=e.target.closest('[data-post-status]');if(status&&!status.disabled){changeStatus(status.dataset.id,status.dataset.postStatus);return}const schedule=e.target.closest('[data-post-schedule]');if(schedule){const post=state.items.find(p=>p.id===schedule.dataset.postSchedule);if(post)openSchedule(post);return}const publish=e.target.closest('[data-post-publish-now]');if(publish){publishNow(publish.dataset.postPublishNow)}});$('#searchInput')?.addEventListener('input',e=>{state.filter=e.target.value;renderList()});$('#statusFilter')?.addEventListener('change',e=>{state.status=e.target.value;renderList()});$('#clearFilters')?.addEventListener('click',()=>{state.filter='';state.status='all';$('#searchInput').value='';$('#statusFilter').value='all';renderList()})}
async function init(){bind();await Promise.allSettled([loadMe(),load()]);document.documentElement.dataset.publishingRuntime='20260809-standalone-v1'}
document.addEventListener('DOMContentLoaded',init);