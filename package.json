{
  "name": "xianjiawei-internal-public-runtime",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "scripts": {
    "build:static": "rm -rf dist && mkdir -p dist/assets/css dist/assets/js && cp index.html dist/index.html && cp assets/css/internal-app.css dist/assets/css/internal-app.css && cp assets/js/internal-app.js dist/assets/js/internal-app.js && if [ -f robots.txt ]; then cp robots.txt dist/robots.txt; fi",
    "deploy": "npm run build:static && wrangler deploy --keep-vars",
    "check": "node --check src/worker.js && node --check assets/js/internal-app.js"
  },
  "dependencies": {
    "jose": "6.0.0"
  },
  "devDependencies": {
    "wrangler": "4.0.0"
  }
}
