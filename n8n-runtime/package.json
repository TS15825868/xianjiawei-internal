{
  "name": "xianjiawei-n8n-runtime",
  "private": true,
  "version": "1.0.0",
  "description": "仙加味 n8n 第一階段測試執行環境；正式營運資料不存放於此。",
  "scripts": {
    "start": "rm -f /tmp/xjw-workflows.json && n8n export:workflow --all --output=/tmp/xjw-workflows.json && node -e \"const fs=require('fs'); const w=JSON.parse(fs.readFileSync('/tmp/xjw-workflows.json','utf8')); console.log('XJW_WORKFLOW_INDEX='+JSON.stringify(w.map(x=>({id:x.id,name:x.name,active:x.active}))));\" && n8n start"
  },
  "dependencies": {
    "n8n": "2.35.7"
  }
}
