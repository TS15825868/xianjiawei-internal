name: Instagram repair probe

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-instagram-identity-20260904.yml'

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
